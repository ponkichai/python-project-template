# new-feature スキル

新機能を DDD → SDD → TDD → AIDD の順で実装する。

## トリガー

「新機能」「new feature」「〜を実装したい」「〜を追加したい」と言われたら起動する。

## 手順

**正本は `docs/workflow.md`。このスキルは Claude Code 用の起動トリガーに過ぎない。**

1. `docs/workflow.md` を読み込む
2. Step 0（DDD：`CONTEXT.md` の用語確認）から Step 4（検証：`make check`）まで、記載の順に実行する
