# python-project-template

AI時代の開発を前提にした、汎用Pythonプロジェクトのスターターテンプレート。
規律の実体をハーネス外の層に置くことで、人間のみの開発でも・どのAIエージェントでも同じ品質保証が効く状態を目指す。

<!--
このファイルはプロジェクトの「ユビキタス言語（用語集）」の正本。
- テンプレートからプロジェクトを開始したら、以下の用語群を自分のドメインの用語に置き換える
- 新しいドメイン概念が出てきたら、実装前にここへ追記する（docs/workflow.md の Step 0）
- コード上の命名はこの表と一致させる（docs/conventions.md 参照）
- 定義は1〜2文で「何であるか」を書く。実装詳細は書かない
-->

## Language

**ハーネス（Harness）**:
AIエージェントを動かす実行環境・製品（Claude Code、Codex、Cursor等）。モデルそのものではなく、ツール実行・hook・権限管理を担う側。
_Avoid_: エージェント（エージェントはハーネス上で動く主体を指す）

**ハーネス非依存**:
規律（lint・型・テスト・機密防御）の実体がハーネス外の層に存在し、(1) 人間だけの開発でも劣化せず、(2) どのハーネスで開いても同じ規律が効く状態。優先順位は (1) > (2)。
_Avoid_: マルチエージェント対応（複数エージェントの協調とは別概念）

**環境層**:
コンテナ・ファイアウォールなど、プロセスの外側から被害範囲を制限する層。devcontainer + egress firewall が該当。
_Avoid_: サンドボックス層（サンドボックスは環境層の実装手段の一つ）

**リポジトリ層**:
git hooks・CI・Makefile・specs/ など、リポジトリ自体に埋め込まれ、gitとPythonがあれば誰にでも効く規律の層。

**ハーネス層**:
特定ハーネス向けの指示・hook・スキル（現状は `.claude/` 配下）。薄いアダプタであるべきで、規律の実体を持ってはならない。
